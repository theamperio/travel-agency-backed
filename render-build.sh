#!/usr/bin/env bash
# Exit on error
set -o errexit

# Install dependencies
npm install

# Clean dist directory if it exists
rm -rf dist

# Build the TypeScript project
npx tsc

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/ 2>/dev/null || true
npm run build

# Print the contents of dist directory for debugging
ls -la dist || echo "dist directory not found"

# Copy any non-TypeScript files to dist if needed
# cp .env dist/